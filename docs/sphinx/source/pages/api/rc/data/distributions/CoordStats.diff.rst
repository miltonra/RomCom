rc.data.distributions.CoordStats.diff
=====================================

.. py:method:: rc.data.distributions.CoordStats.diff(path, stats)

   Compare ``self`` to ``stats``, returning the difference.

   :param path: The Path to this Table, overwritten if existing.
   :param stats: The ``Stats`` to compare to.

   Returns: The difference ``self - stats``, stored in ``path``.

